# carteirinha

# Running
## With docker-compose
> docker-compose build  
> docker-compose up -d

## Local install
> create a .env (see a example in `.env.example`)  
> npm install   
> npm start

# running tests
> npm run test

