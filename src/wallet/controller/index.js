const { createCard } = require('./createCard')
const { createwallet } = require('./createwallet')
const { findCards } = require('./findCards')
const { findWallet } = require('./findWallet')
const { findWallets } = require('./findWallets')
const { removeWallet } = require('./removeWallet')
const { shop } = require('./shop')

module.exports = {
  createCard,
  createwallet,
  findCards,
  findWallet,
  findWallets,
  removeWallet,
  shop
}
